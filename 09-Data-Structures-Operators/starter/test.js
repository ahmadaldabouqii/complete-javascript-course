'use strict';
function Person(id, first, last) {
  this.id_Num = id;
  this.f_name = first;
  this.last_name = last;
}

const abdullah = new Person(999771, 'adullah', 'maraai');
console.log(abdullah);
